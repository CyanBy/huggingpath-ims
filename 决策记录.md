# 决策记录

## 2026-09-23 · HP 四个历史对话已完整接续

- 已读取同一 HP 项目下的 4 个历史任务：`HuggingPath AI界面设计4.0`、`KIMI HP文档`、`讨论融合对话Agent方案`、`KIMI界面设计3.0`，共 126 个任务轮次（包括完成、失败和中断轮次）。后续应以 **4.0 的最新实现与当前仓库**为准；3.0 和 Agent 方案对话主要用于追溯设计原因，不能用旧结论覆盖新实现。
- 产品定位已稳定：**对话串联流程，页面执行工作**；病理阅片、ROI、图层等视觉精细操作继续留在原页面。AI 助手与 HP 使用同一份数据，不做两套数据同步。当前仍是前端设计图工程，不接真实 LLM API，以可控示例数据模拟接入后的体验，回答和科研数字不得编造。
- 对话与项目的关系：自由对话可以不属于项目；项目是病理业务实体而不是对话的前置条件。项目可从 HP 或助手创建，读写同一 store。分析任务最小归属仍是 WSI/Case，项目可选。
- 写操作边界：上传、分析以及后来加入的项目创建/切片纳入等会改变平台数据的 Agent 操作，都必须经过“预览/计划卡 → 用户确认 → 真执行”的闸门；只读查询、查看和解读无需确认。未来接 LLM 时，模型负责意图理解、参数抽取、多步规划和文案；本地工具层仍必须负责 schema、校验、确认闸门、执行器与结果回喂。
- AI 助手当前已实现的主要闭环：Codex/豆包式会话与草稿制、项目/会话管理、置顶与跨对话搜索、时间分组、模拟流式/停止、复制/重新生成/编辑重发、完成 toast/未读红点/标题提示、分析确认卡与任务生命周期/完成回话、HP 情境入口、讨论对象与实体跳转、行为日志、STAD 两段式回复与产物预览、会话文件管理器、拖拽上传、切片进入 WSI 管理前确认、模拟 Agent 创建项目、结构化工具调用步骤。视觉改动必须实际截图确认，不能只检查 DOM 或类名。
- 明确暂缓/未做：全局对话抽屉（C1）放后面；归档机制和快捷键暂不做；未接真实 API；Skill 1/2/3 只是入口占位。真实 LLM 接入时应优先替换 `mockReply`/正则/剧本匹配，保留现有工具层、闸门和执行框架。
- `KIMI HP文档` 的最新验收范围：`HuggingPath功能清单.xlsx` 已从 51 行缩为 49 行，Case/研究项目不在本次验收范围；已删除 WSI“Case 联动”和创建任务“快捷建 Case”，其余页面上仍存在的 Case 元素标记为“本期不验收”。当前文件路径：`/Users/npc77/Documents/ChatGPT/HuggingPath/需求与设计文档/HuggingPath功能清单.xlsx`。
- 当前 Git 事实（读取当时）：工作区干净，`main` 比 `origin/main` **领先 8 个本地提交**；HEAD=`cac0aba`。8 个提交涵盖结构化思考步骤、传输预览“清除记录”及角标联动、多 Case 分组与文件夹式收起、分析模型区紧凑化、跑马灯结构性修复。未经用户明确要求不要推送。
- 4.0 对话最后一项未完成请求：用户提供 `/Users/npc77/Documents/ChatGPT/HuggingPath/0901验收内容.docx`，要求“领导希望按照这个样式再做一版，你先说你的理解”。原任务因 Kimi 周额度 403 和 `k3` 模型不支持而失败，尚未完成理解说明或新版制作；不要误认为已处理。

## 2026-09-23 · HP 简易用户说明书方向确认

- 用户确认：以 `/Users/npc77/Documents/ChatGPT/HuggingPath/0901验收内容.docx` 为样式参考，为 HuggingPath 另做一版 Word 文档。
- 文档定位是**简易用户说明书**，不是验收测试用例；需要大量当前 HP 页面截图、红框/箭头/圆圈标注和逐步操作说明。
- 不写“验收标准”“预期结果”“通过/不通过”等测试判定内容。参考文档的核心是章节编号、功能说明、步骤和截图证据，不能误做回 Excel 功能清单或 AI 测试文档。
- 内容继续遵循本期范围：登录与管理后台、WSI 管理与上传、传输队列、创建与配置分析任务、分析任务列表、分析工作台和结果查看；Case 与研究项目不作为本期说明主体。
- 已完成 Word 文档：`/Users/npc77/Documents/ChatGPT/HuggingPath/需求与设计文档/HuggingPath简易用户说明书.docx`。成品为 A4 纵向 36 页，含 8 个章节、33 张当前界面截图、106 条分步操作说明；章节、步骤与图片编号均从 1 开始。已完成 DOCX 包完整性检查、逐页渲染检查和 macOS Quick Look 中文字体复核。

## 2026-09-23 · Demo 环境按说明书实操复核

- 已在 `https://local.sqray.com/huggingpath-web/#/home` 使用 `superadmin` 完成登录、后台管理、WSI、传输队列、分析任务、工作台结果、模型中心、新建任务提交前流程和退出登录的实际操作。
- 为保护现有样例数据，本次只操作到最终提交前：未上传文件、未提交分析、未新增用户/角色/机构、未保存系统设置，也未删除任何现有数据；因此没有产生需要清理的测试数据。
- 正常项：登录/退出、后台各模块访问和新增表单、WSI/任务搜索、上传弹窗、任务对象的 WSI/Case/研究项目切换、已完成任务工作台、分析记录、TME 定量、特征概览、倍率切换和热力图开关均可操作。
- 高优先级问题：失败任务 `64145418199232` 在列表显示“80 张切片、失败”，详情却先后只呈现部分对象，顶部显示“分析队列/待分析”，多个切片显示“分析完成”，且无法重试；页面同时连续产生大量 `Uncaught (in promise)`，任务状态、对象数与详情状态不一致。
- 其他问题：模型详情页仍是“模型详情页待实现”占位页；数据页常先显示 0 或“暂无数据”，约 1–4 秒后才出现真实内容；完成任务分析结果约 10–15 秒才加载完成；“准备上传”的 WSI 仍显示可点击“查看”但点击无明显反馈。
- 当前 Demo 与说明书存在差异：上传弹窗只有统一“选择文件”，未见独立 DICOM 文件夹入口；任务操作文案为“打开工作台/查看任务”；结果页签实际为“TME 定量/特征概览/报告-Agent”；新建任务页可直接切换 WSI、Case、研究项目，并且每个已有 WSI 行旁出现“删除”操作。下次修订说明书应以 Demo 实际文案与页面结构为准。

## 2026-09-20 · HP 文档目录结构（~/Documents/HuggingPath项目文件/）

- `需求与设计文档/`：PRD 截图、角色权限矩阵
- `原型代码/`：HTML 原型片段（WSI关联CASE.html）
- `原型工程/`：旧原型项目 hp-wsi-claude（544M）、pathopen、huggingpath-ims-src.tgz
- `科研素材/stad_pm_package/`：STAD 交付包（见下条）
- `设计图/`：暂空——平台UI设计图深度扫描未发现可确证为 HP 的文件（全部为 MIIS 或无法甄别）
- 活跃代码仓库保持在 `~/Documents/github/huggingpath/huggingpath-ims`，不迁移
- 非 HP 文件（MIIS 沟通纪要、部署方案）已移至 `~/Documents/MIIS文件/`

## 2026-09-20 · STAD 科研交付包（模拟数据素材）

- **素材路径**：`/Users/npc77/Documents/HuggingPath项目文件/03_科研素材/stad_pm_package/`（仓库外，勿提交；2026-09-20 文档归类后路径）
- **来源与用途**：用户 AI 同事（TME 科研侧）提供，供 HP 前端原型做**模拟数据参考**：16 张图（小提琴/t-SNE/ROC/OS 生存）、9 份数据表、6 段真实对话记录、`PROMPT.md`（TME Agent 演示提示词 + 5 个演示任务剧本）。
- **关键数字（演示时引用，不许编）**：TCGA-STAD 375 例（G1/G2 144 vs G3 231）；951 项 TME 特征（851/845 合格）；差异 Top10 全部 FDR<0.05、|d|>0.64；独立留出集 AUROC 0.777（CI 0.687–0.861），Top3 0.696 vs 全量 0.740；亚型 CIN 192 / MSI 59 / GS 44 / EBV 27 / POLE 6（POLE 仅探索性）。
- **同事设计的助手 UX 范式（后续升级方向）**：回复=可折叠「思考过程」+「最终交付」（正文+产物超链接卡片，图片可预览）。我们现有差距：无持久推理链展示、无图片/数据类产物卡片。
- **接法（已定 2026-09-20，用户拍板直接开工）**：按进阶方案实施完毕——消息模型升级两段式（思考折叠块+图/表/文件产物卡+图片预览弹窗），`src/lib/stadScript.ts` 灌入 5 段 STAD 剧本，素材在 `public/stad/`，预置「STAD 队列 TME 研究」项目与 5 轮演示会话（ensureStadDemoSeed 独立幂等）。回退点：tag `pre-stad-checkpoint`。
- **缺口**：`case_views/` 为空；对话内产物链接是占位符，接入时需映射为本地静态资源。

## 2026-09-14 · 上下文估算校准

- 分母按当前模型实际上下文窗口计（1M 窗口按 1M 算），先粗算累计 tokens 再换算百分比。已写入全局 AGENTS.md。
